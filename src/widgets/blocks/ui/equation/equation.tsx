import { twMerge } from "tailwind-merge";

export const Equation = ({ expression, className }: { expression: string, className?: string }) => (
  // <MathJax className={twMerge("mb-4", className)}>{`\\[${expression}\\]`}</MathJax>
  <>Equation</>
)